---
# tasks file for plex_update

- name: stop plexmedia service
  service:
    name: plexmediaserver
    state: stopped

- name: update plexrpm
  shell: /usr/bin/yum localupdate -y /tmp/plexmediaserver*.rpm
  ignore_errors: True

- name: Delete file wildcard
  shell: /usr/bin/rm -rf /tmp/plexmediaserver*.rpm
  ignore_errors: True

- name: upgrade all packages
  yum:
    name: '*'
    state: latest

- name: Check for reboot hint.
  shell: LAST_KERNEL=$(rpm -q --last kernel | awk 'NR==1{sub(/kernel-/,""); print $1}'); CURRENT_KERNEL=$(uname -r); if [ $LAST_KERNEL != $CURRENT_KERNEL ]; then echo 'reboot'; else echo 'no'; fi
  ignore_errors: true
  register: reboot_hint

- name: Rebooting ...
  command: shutdown -r now "Reboot required for updated kernel"
  async: 0
  poll: 0
  sudo: true
  ignore_errors: true
  when: reboot_hint.stdout.find("reboot") != -1
  register: rebooting

- name: Wait for thing to reboot...
  pause: seconds=45
  when: rebooting|changed

- name: Start service plexmediaserver, if not running
  service:
    name: plexmediaserver
    state: started
